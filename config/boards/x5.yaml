board:
  profile: x5
  model: D-Robotics X5 MD V1.2
  host: 10.112.10.100
  python: "3.10"
  cpu: 8x Cortex-A55
  memory_visible_gib: 6.9
  bpu_cores: 1
  role: lightweight-edge-worker
  prompt: >-
    This is the constrained X5 profile. Prefer remote or LAN inference, bounded
    context, small outputs, and local preprocessing. Do not assume a local LLM is
    available merely because the BPU runtime exists.
