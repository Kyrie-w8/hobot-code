board:
  profile: s600
  model: D-Robotics RDK S600 MCB V0p2
  host: 10.112.10.106
  python: "3.12"
  cpu: 18x Cortex-A78AE
  memory_visible_gib: 10.0
  bpu_cores: 4
  role: coordinator-and-inference-worker
  prompt: >-
    This S600 can coordinate other nodes and perform heavier preprocessing, but
    accelerator memory and normal Linux memory are separate pools. Route work using
    measured capacity and never infer arbitrary LLM compatibility from BPU presence.
