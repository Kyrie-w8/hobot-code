# 为 Hobot Code 贡献

感谢你改进 Hobot Code。这个项目直接运行在资源受限的机器人开发板上，变更应同时满足交互一致性、板端可部署性和硬件安全边界。

## 开发原则

- 保留 Pi 的原生交互语义，板卡差异应集中在 RDK 扩展、Prompt、知识库和部署层。
- 以实机或可复现测试为证据，不把设备节点存在等同于模型已经完成转换或 BPU 验收。
- 保持改动聚焦；只有在确实减少复杂度或重复时才增加抽象。
- 默认配置保持保守，不静默放宽 root、系统路径、Shell 或第三方工具的权限。
- 用户可见行为、配置项、目录或安全边界发生变化时，同步更新 README 和相关文档。

## 开发环境

需要 Git、`make`、Node.js 22.13 或更新版本、POSIX Shell、`curl`、`tar`、`gzip`，以及 `sha256sum` 或 `shasum`。执行完整发行构建还需要访问上游归档，或准备[经过校验的本地缓存](docs/configuration.md#构建覆盖)。

克隆后先运行基线检查：

```bash
make check
```

该命令包含 Shell 与 JSON 语法检查、Node 测试、知识库与专家 Prompt 预算验证、品牌、文档链接和版本一致性检查，以及扩展源码语法与模块依赖检查。完整发行包的文件集合与清单在 `make release` 阶段校验。

## 变更流程

1. 从最新代码创建范围明确的分支。
2. 先为行为变化补充或调整测试，再实现最小完整改动。
3. 检查错误路径、超时、资源上限、权限和非交互模式。
4. 同步更新受影响的文档与示例。
5. 运行 `make check`；涉及打包时，使用 `HOBOT_CODE_ALLOW_DIRTY_BUILD=1 make release` 验证本地改动。
6. 在受影响的 RDK 板卡上完成安装、启动和关键工作流验证。

可以单独运行某个测试文件以缩短反馈周期：

```bash
node --test tests/provider-stream.test.mjs
node --test tests/side-agent.test.mjs
```

最终提交前仍应运行完整的 `make check`。

正式 `make release` 要求工作区干净，使归档能够追溯到确定提交。`HOBOT_CODE_ALLOW_DIRTY_BUILD=1` 只用于本地开发验证，生成的 dirty 包不应对外发布。

## 变更类型

### Provider 与流式协议

覆盖 SSE 分片、thinking、文本、工具参数、usage、结束事件、普通 JSON 回退、超时、响应上限和异常 Unicode。不要用无限缓冲替代流式解析。

### 板卡知识与专家 Prompt

知识条目必须标明适用板卡、RDK OS 线路和可追溯来源。更新知识时同步维护 `knowledge/manifest.json` 的版本与日期。专家 Prompt 只放稳定的板端决策规则，详细资料应通过工具或 Skills 按需加载。

### 权限、Hook 与设备操作

新增修改能力时必须说明目标、确认条件、root 与非交互行为，以及是否触及 `/boot`、`/dev`、`/etc`、`/proc`、`/sys`、`/usr` 或 `/var/lib`。检测规则不是沙箱；测试需要同时覆盖允许、确认、拒绝和绕过尝试。

### Pi 运行时与工具升级

升级 Pi、`fd` 或 `ripgrep` 时，同步更新对应锁文件、来源、SHA256 和第三方许可证，并完成 `make check`、发行包校验与至少一台目标板卡回归。不要依赖运行时自更新绕过版本锁。

### 安装与迁移

安装器改动必须验证首次安装、普通用户通过 `sudo` 安装、root 直接安装、升级中断、空间不足、活动进程阻断、旧布局迁移和有完整备份时的回滚。不得在失败路径丢失用户配置或状态。

## 提交内容

请勿提交：

- API token、私钥、认证文件或真实 `hobot.env`。
- `dist/` 发行产物、临时会话、数据库、日志或设备采样数据，除非维护者明确要求。
- 与当前变更无关的格式化、重命名或生成文件。

提交消息应简洁说明行为变化。Pull request 描述至少包含问题、方案、风险、验证命令和板卡验证结果；无法执行的验证要明确说明原因。

## 提交检查表

- [ ] 变更范围单一，代码和命名清晰。
- [ ] 新行为及失败路径有测试覆盖。
- [ ] `make check` 通过。
- [ ] 用户可见行为与文档一致。
- [ ] 未提交密钥、状态数据或发行产物。
- [ ] 硬件相关变更注明已验证的板卡与 RDK OS。
- [ ] 第三方来源、锁文件、SHA256 和许可证保持一致。

安全问题不要先提交公开 issue，请按[安全说明](SECURITY.md#报告安全问题)私下报告。
